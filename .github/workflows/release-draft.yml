name: Release Draft

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to create or update, for example v0.5.0"
        required: true
  push:
    tags: ["v*"]

permissions:
  contents: write

concurrency:
  group: release-draft-${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
  cancel-in-progress: false

env:
  RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}

jobs:
  macos:
    name: Build macOS release artifact
    runs-on: macos-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate release version and updater signing key
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        run: |
          node scripts/sync_version.mjs --check --tag "$RELEASE_TAG"
          npm run test:release
          if [ -z "$TAURI_SIGNING_PRIVATE_KEY" ]; then
            echo "::error title=Missing updater key::Add TAURI_SIGNING_PRIVATE_KEY to the repository Actions secrets."
            exit 1
          fi

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"
          cache-dependency-path: app/backend/requirements.txt

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.2
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "pnpm"
          cache-dependency-path: app/desktop/pnpm-lock.yaml

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install backend dependencies
        working-directory: app/backend
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt pyinstaller

      - name: Run backend tests
        working-directory: app/backend
        run: python -m unittest discover -s tests

      - name: Install desktop dependencies
        working-directory: app/desktop
        run: pnpm install --frozen-lockfile

      - name: Build macOS backend sidecar
        working-directory: app/desktop
        run: pnpm run backend:sidecar

      - name: Run Rust tests
        run: cargo test --manifest-path app/desktop/src-tauri/Cargo.toml

      - name: Smoke test packaged backend sidecar
        working-directory: app/desktop
        timeout-minutes: 3
        run: pnpm run backend:sidecar:smoke 2>&1 | tee smoke.log

      - name: Download cloudflared connector
        working-directory: app/desktop
        run: pnpm run cloudflared:sidecar

      - name: Build macOS Tauri app and installer
        working-directory: app/desktop
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        run: pnpm exec tauri build --ci --bundles app,dmg

      - name: Smoke test macOS desktop launcher
        working-directory: app/desktop
        timeout-minutes: 3
        run: pnpm run macos:launcher:smoke

      - name: Stage macOS release artifacts
        run: |
          stage="app/desktop/dist-artifacts/macos"
          bundle_root="app/desktop/src-tauri/target/release/bundle"
          rm -rf "$stage"
          mkdir -p "$stage"

          ditto -c -k --sequesterRsrc --keepParent "$bundle_root/macos/Listency.app" "$stage/Listency-macos.zip"

          if [ -d "$bundle_root/dmg" ]; then
            find "$bundle_root/dmg" -maxdepth 1 -type f -name "*.dmg" -exec cp {} "$stage/" \;
          fi

          updater_archive="$(find "$bundle_root/macos" -maxdepth 1 -type f -name "*.app.tar.gz" -print -quit)"
          if [ -z "$updater_archive" ] || [ ! -f "$updater_archive.sig" ]; then
            echo "Signed macOS updater artifact was not produced." >&2
            exit 1
          fi
          cp "$updater_archive" "$updater_archive.sig" "$stage/"
          case "$(uname -m)" in
            arm64) updater_target="darwin-aarch64" ;;
            x86_64) updater_target="darwin-x86_64" ;;
            *) echo "Unsupported macOS updater architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          printf '%s\n' "$updater_target" > "$stage/UPDATER_TARGET.txt"
          basename "$updater_archive" > "$stage/UPDATER_ARTIFACT.txt"

          cp app/desktop/smoke.log "$stage/smoke.log"
          {
            echo "platform=macos"
            echo "release_tag=$RELEASE_TAG"
            echo "signed=false"
            echo "notarization_configured=false"
            echo "updater_signed=true"
          } > "$stage/SIGNING_STATUS.txt"

          cd "$stage"
          shasum -a 256 $(find . -maxdepth 1 -type f ! -name "SHA256SUMS.txt" -print | sort) > SHA256SUMS.txt
          cat SHA256SUMS.txt

      - name: Upload macOS release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: listency-macos-${{ env.RELEASE_TAG }}
          if-no-files-found: error
          path: app/desktop/dist-artifacts/macos/**

  windows:
    name: Build Windows release artifact
    runs-on: windows-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate release version and updater signing key
        shell: pwsh
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        run: |
          node scripts/sync_version.mjs --check --tag "$env:RELEASE_TAG"
          npm run test:release
          if ([string]::IsNullOrWhiteSpace($env:TAURI_SIGNING_PRIVATE_KEY)) {
            Write-Output "::error title=Missing updater key::Add TAURI_SIGNING_PRIVATE_KEY to the repository Actions secrets."
            exit 1
          }

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"
          cache-dependency-path: app/backend/requirements.txt

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.2
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "pnpm"
          cache-dependency-path: app/desktop/pnpm-lock.yaml

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install backend dependencies
        working-directory: app/backend
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt pyinstaller

      - name: Run backend tests
        working-directory: app/backend
        run: python -m unittest discover -s tests

      - name: Install desktop dependencies
        working-directory: app/desktop
        run: pnpm install --frozen-lockfile

      - name: Build Windows backend sidecar
        working-directory: app/desktop
        run: pnpm run backend:sidecar

      - name: Run Rust tests
        run: cargo test --manifest-path app/desktop/src-tauri/Cargo.toml

      - name: Smoke test packaged backend sidecar
        working-directory: app/desktop
        timeout-minutes: 3
        run: |
          pnpm run backend:sidecar:smoke *> smoke.log
          $exitCode = $LASTEXITCODE
          Get-Content smoke.log
          if ($exitCode -ne 0) {
            $message = (Get-Content smoke.log -Raw).Replace('%', '%25').Replace("`r", '%0D').Replace("`n", '%0A')
            Write-Output "::error title=Packaged backend smoke failed::$message"
            exit $exitCode
          }

      - name: Download cloudflared connector
        working-directory: app/desktop
        run: pnpm run cloudflared:sidecar

      - name: Build Windows Tauri installer
        working-directory: app/desktop
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        run: pnpm exec tauri build --ci --bundles nsis

      - name: Package portable Windows app
        working-directory: app/desktop
        run: pnpm run windows:portable

      - name: Verify Windows GUI subsystem
        working-directory: app/desktop
        run: pnpm run windows:subsystem:check

      - name: Smoke test Windows desktop launcher
        working-directory: app/desktop
        timeout-minutes: 3
        run: pnpm run windows:launcher:smoke

      - name: Stage Windows release artifacts
        shell: pwsh
        run: |
          $release = Resolve-Path "app/desktop/src-tauri/target/release"
          $stage = "app/desktop/dist-artifacts/windows"
          Remove-Item $stage -Recurse -Force -ErrorAction SilentlyContinue
          New-Item -ItemType Directory -Force $stage | Out-Null

          Copy-Item "$release/portable" "$stage/portable" -Recurse

          if (Test-Path "app/desktop/smoke.log") {
            Copy-Item "app/desktop/smoke.log" "$stage/smoke.log"
          }

          $nsis = Join-Path $release "bundle/nsis"
          if (Test-Path $nsis) {
            Copy-Item "$nsis/*.exe" $stage -ErrorAction SilentlyContinue
          }

          $updater = Get-ChildItem $nsis -Filter "*.exe" | Select-Object -First 1
          if ($null -eq $updater -or -not (Test-Path "$($updater.FullName).sig")) {
            throw "Signed Windows updater artifact was not produced."
          }
          Copy-Item "$($updater.FullName).sig" $stage
          $updater.Name | Set-Content -Path "$stage/UPDATER_ARTIFACT.txt" -Encoding utf8
          switch ($env:PROCESSOR_ARCHITECTURE) {
            "AMD64" { "windows-x86_64" | Set-Content -Path "$stage/UPDATER_TARGET.txt" -Encoding utf8 }
            "ARM64" { "windows-aarch64" | Set-Content -Path "$stage/UPDATER_TARGET.txt" -Encoding utf8 }
            default { throw "Unsupported Windows updater architecture: $env:PROCESSOR_ARCHITECTURE" }
          }

      - name: Write Windows checksums
        shell: pwsh
        run: |
          $root = Resolve-Path "app/desktop/dist-artifacts/windows"
          $statusPath = Join-Path $root "SIGNING_STATUS.txt"
          @(
            "platform=windows",
            "release_tag=$env:RELEASE_TAG",
            "signed=false",
            "updater_signed=true"
          ) | Set-Content -Path $statusPath -Encoding utf8

          $files = Get-ChildItem $root -File -Recurse | Where-Object { $_.Name -ne "SHA256SUMS.txt" }
          $lines = $files | Sort-Object FullName | ForEach-Object {
            $relative = $_.FullName.Substring($root.Path.Length + 1).Replace("\", "/")
            $hash = (Get-FileHash -Algorithm SHA256 $_.FullName).Hash.ToLower()
            "$hash  $relative"
          }
          $checksumPath = Join-Path $root "SHA256SUMS.txt"
          Set-Content -Path $checksumPath -Value $lines -Encoding utf8
          Get-Content $checksumPath

      - name: Upload Windows release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: listency-windows-${{ env.RELEASE_TAG }}
          if-no-files-found: error
          path: app/desktop/dist-artifacts/windows/**

  draft-release:
    name: Create GitHub draft release
    runs-on: ubuntu-latest
    needs: [macos, windows]
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate release version
        run: node scripts/sync_version.mjs --check --tag "$RELEASE_TAG"

      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts

      - name: Build release notes and checksums
        run: |
          mkdir -p release-assets

          find release-artifacts -type f -print0 |
            sort -z |
            xargs -0 shasum -a 256 > release-assets/SHA256SUMS-files.txt

          {
            echo "# Listency $RELEASE_TAG"
            echo
            echo "Draft release generated by GitHub Actions."
            echo
            echo "## Artifacts"
            echo
            echo "- macOS: app zip plus DMG when Tauri produces one."
            echo "- Windows: NSIS installer plus portable app folder."
            echo "- Automatic updates: signed Tauri updater packages plus latest.json."
            echo "- Checksums: per-platform SHA256SUMS.txt and top-level SHA256SUMS-all.txt."
            echo
            echo "## Signing"
            echo
            echo "The macOS and Windows applications are intentionally unsigned at the operating-system level. Tauri updater packages are cryptographically signed so installed apps can verify automatic updates. Each platform archive includes SIGNING_STATUS.txt."
            echo
            echo "## macOS unsigned build note"
            echo
            echo 'If macOS shows `"Listency" is damaged and can'\''t be opened`, Gatekeeper is blocking the unsigned downloaded app. For builds downloaded from this repository, remove the quarantine flag after extracting or installing the app:'
            echo
            echo '```bash'
            echo 'xattr -dr com.apple.quarantine /path/to/Listency.app'
            echo '```'
            echo
            echo "This prompt is expected for unsigned builds."
          } > RELEASE_NOTES.md

          for artifact_dir in release-artifacts/*; do
            if [ -d "$artifact_dir" ]; then
              archive_name="$(basename "$artifact_dir").zip"
              (cd release-artifacts && zip -r "../release-assets/$archive_name" "$(basename "$artifact_dir")")
            fi
          done

          cp RELEASE_NOTES.md release-assets/RELEASE_NOTES.md
          node scripts/generate_update_manifest.mjs \
            --artifacts-dir release-artifacts \
            --release-assets-dir release-assets \
            --output release-assets/latest.json \
            --notes RELEASE_NOTES.md \
            --repository "$GITHUB_REPOSITORY" \
            --tag "$RELEASE_TAG" \
            --version "${RELEASE_TAG#v}"

          find release-assets -maxdepth 1 -type f ! -name "SHA256SUMS-all.txt" -print0 |
            sort -z |
            xargs -0 shasum -a 256 > release-assets/SHA256SUMS-all.txt

          cat release-assets/SHA256SUMS-all.txt

      - name: Create or update draft release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          mapfile -t assets < <(find release-assets -maxdepth 1 -type f -print | sort)
          if [ "${#assets[@]}" -eq 0 ]; then
            echo "No release assets found." >&2
            exit 1
          fi

          body="$(cat release-assets/RELEASE_NOTES.md)"
          release_id="$(
            gh api "repos/${GITHUB_REPOSITORY}/releases?per_page=100" \
              --jq ".[] | select(.tag_name == \"${RELEASE_TAG}\") | .id" |
              head -n 1
          )"

          if [ -z "$release_id" ]; then
            if tag_release_id="$(gh api "repos/${GITHUB_REPOSITORY}/releases/tags/${RELEASE_TAG}" --jq '.id' 2>/dev/null)"; then
              release_id="$tag_release_id"
            fi
          fi

          if [ -n "$release_id" ] && ! [[ "$release_id" =~ ^[0-9]+$ ]]; then
            echo "Ignoring invalid release id lookup result: $release_id" >&2
            release_id=""
          fi

          if [ -n "$release_id" ]; then
            echo "Updating existing release $RELEASE_TAG ($release_id), preserving its draft/published state."
            gh api \
              --method PATCH \
              "repos/${GITHUB_REPOSITORY}/releases/${release_id}" \
              -f name="Listency ${RELEASE_TAG}" \
              -f body="$body" >/dev/null
          else
            echo "Creating draft release $RELEASE_TAG."
            prerelease=false
            case "$RELEASE_TAG" in
              *alpha*|*beta*|*rc*) prerelease=true ;;
            esac

            release_id="$(gh api \
              --method POST \
              "repos/${GITHUB_REPOSITORY}/releases" \
              -f tag_name="$RELEASE_TAG" \
              -f target_commitish="$GITHUB_SHA" \
              -f name="Listency ${RELEASE_TAG}" \
              -f body="$body" \
              -F draft=true \
              -F prerelease="$prerelease" \
              --jq '.id')"
          fi

          for asset in "${assets[@]}"; do
            name="$(basename "$asset")"
            echo "Uploading $name."

            while IFS= read -r existing_asset_id; do
              if [ -n "$existing_asset_id" ]; then
                gh api \
                  --method DELETE \
                  "repos/${GITHUB_REPOSITORY}/releases/assets/${existing_asset_id}" >/dev/null
              fi
            done < <(gh api "repos/${GITHUB_REPOSITORY}/releases/${release_id}/assets?per_page=100" --jq ".[] | select(.name == \"$name\") | .id")

            encoded_name="$(python3 -c 'import sys, urllib.parse; print(urllib.parse.quote(sys.argv[1]))' "$name")"
            curl --fail-with-body -sS \
              -X POST \
              -H "Authorization: Bearer ${GH_TOKEN}" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/octet-stream" \
              --data-binary @"$asset" \
              "https://uploads.github.com/repos/${GITHUB_REPOSITORY}/releases/${release_id}/assets?name=${encoded_name}" >/dev/null
          done
